services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: synergia
      POSTGRES_USER: synergia
      POSTGRES_PASSWORD: synergia-local-only
    ports:
      - "5432:5432"
    volumes:
      - synergia-postgres:/var/lib/postgresql/data

volumes:
  synergia-postgres:
