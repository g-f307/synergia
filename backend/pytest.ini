[pytest]
pythonpath = .
markers =
    integration: requer PostgreSQL e testa persistência real
    real_authorization: exercita a dependência real de autenticação e autorização
    security: regressão dedicada de autenticação, autorização e exposição
testpaths = tests
